// Nativo (iOS/Android): usa a lib real, baseada em WebView.
// A versão para navegador fica em SignaturePad.web.tsx — o Metro escolhe
// o arquivo certo automaticamente pela extensão, conforme a plataforma.
import SignatureScreen from 'react-native-signature-canvas';

export default SignatureScreen;
